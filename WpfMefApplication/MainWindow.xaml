<Window x:Class="WpfMefApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMefApplication"
        xmlns:viewModels="clr-namespace:WpfMefApplication"
        d:DataContext="{d:DesignInstance {x:Type viewModels:MainViewModel}}"
        xmlns:userControls="clr-namespace:WpfMefApplication.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <userControls:PersonUserControl DataContext="{Binding PersonViewModel}" />
    </Grid>
</Window>
